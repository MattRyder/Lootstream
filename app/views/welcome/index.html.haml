.col-xs-12.col-sm-6
  %h2 Bringing the metagame to Livestreams
  %p Lootstream is a service that provides gambling services to livestreamed entertainment.
.col-xs-12.col-sm-4.authentication
  - unless user_signed_in?
    .row
      - view = params[:code] ? 'ajax_sign_up' : 'new_user'
      = render "devise/registrations/#{view}"
    .registration.row
      %h3.welcome Sign in
      = render 'devise/sessions/sign_in_form', resource: User.new, resource_name: 'user'